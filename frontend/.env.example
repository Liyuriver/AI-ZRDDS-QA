# FastAPI base URL used by the browser client.
VITE_API_BASE_URL=/api/v1
VITE_API_PROXY_TARGET=http://127.0.0.1:8000

# Keep Mock enabled until the backend API contract is ready for integration.
VITE_USE_MOCK=true

# Request timeout in milliseconds.
VITE_API_TIMEOUT=30000
